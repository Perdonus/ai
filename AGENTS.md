# Agent Notes

- Use `cmd.exe` by default for shell commands in this repository.
- Avoid PowerShell because it is noticeably laggy on this machine and can load the PC unnecessarily.
- Use PowerShell only as a last resort when `cmd.exe` cannot perform the required task.
