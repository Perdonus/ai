{
  "id": "git-clone",
  "name": "Git Clone",
  "description": "Clone a git repository into a target folder.",
  "kind": "command",
  "entry": "cmd.exe",
  "system_prompt": "Use this when you need to clone a repository before editing, building, or installing a tool or widget.",
  "parameter_hints": {
    "repo": "repository url",
    "target_path": "absolute target folder path"
  },
  "args_template": [
    "/d",
    "/c",
    "git clone --progress \"{{repo}}\" \"{{target_path}}\""
  ],
  "version": "0.1.0"
}
